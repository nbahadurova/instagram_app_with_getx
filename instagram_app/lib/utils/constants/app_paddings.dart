import 'package:flutter/material.dart';


class AppPaddings {
  AppPaddings._();

  static const v10h20 = EdgeInsets.symmetric(horizontal: 20,vertical: 10);
  static const h25 = EdgeInsets.symmetric(horizontal: 25);
  static const h15 = EdgeInsets.symmetric(horizontal: 15);
  static const h10 = EdgeInsets.symmetric(horizontal: 10);
  static const t10 = EdgeInsets.only(top: 10);
  static const b10 = EdgeInsets.only(bottom: 10);
}