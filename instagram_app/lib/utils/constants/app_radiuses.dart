import 'package:flutter/material.dart';

class AppRadiuses {
  AppRadiuses._();
  static const a5 = BorderRadius.all(Radius.circular(5));
  static const a35 = BorderRadius.all(Radius.circular(35));
}
